/* eslint-disable */

var db = require('../db');

module.exports = {
  // a function which produces all the messages
  getAll: function () {
    // return [{username: 'userTest', text: 'this is the test', roomname: 'room1'}];
    get()
  },

  // a function which can be used to insert a message into the database
  create: function () {
    post()
  }
};
